import { ValidecongeService } from './valideconge.service';

describe('ValidecongeService', () => {
  it('should create an instance', () => {
    expect(new ValidecongeService()).toBeTruthy();
  });
});
